% Generated by roxygen2 (4.0.0): do not edit by hand

\name{umx.twin-package}
\alias{umx.twin-package}
\alias{umx.twin}
\docType{package}
\title{
umx.twin
}
\description{
A library of Helper functions for doing twin modeling in OpenMx. If you are just
    starting, see the examples below:
}
\details{
Package: \tab umx\cr
Version: \tab 0.75\cr
License: \tab GPL-3\cr
URL: \tab \url{http://bitbucket.com/tbates/umx.twin}\cr
Depends: \tab R (>= 2.11.0), umx, OpenMx (>= 1.3.0),\cr
Suggests: \tab \cr
Collate: \tab 'build_twin.r' 'deprecated.r' 'reporting.r'
'umx.lib.R' 'plot.r' 'misc.r'\cr
Packaged: \tab 2013-08-22 22:49:21 UTC; tim\cr
Built: \tab R 2.15.2; ; 2013-08-22 22:49:22 UTC; unix\cr

install.packages("devtools")\cr
library("devtools")\cr
install_github("tbates/umx")\cr
library("umx")\cr

A good place to start is below, or ?umxACE
}
\author{
Tim Bates

Maintainer: Who to complain to <timothy.c.bates@gmail.com>
}
\keyword{ package }

\references{
  - \url{http://openmx.psyc.virginia.edu/}
}

\examples{
# The total sample has been subdivided into a young cohort, aged 18-30 years, and an older cohort aged 31 and above.
# Cohort 1 Zygosity is coded as follows
# 1 == MZ females 2 == MZ males 3 == DZ females 4 == DZ males 5 == DZ opposite sex pairs
# ?twinData to learn about this data set
require(OpenMx)
data(twinData)
twinData$ZYG = factor(twinData$zyg, levels = 1:5, labels = c("MZFF", "MZMM", "DZFF", "DZMM", "DZOS"))
# Select DVS: twin 1 vars followed by twin 2 vars
selDVs = c("bmi1","bmi2")
mzData <- as.matrix(subset(twinData, ZYG == "MZFF", selDVs))
dzData <- as.matrix(subset(twinData, ZYG == "DZFF", selDVs))
m1 = umxACE(selDVs = selDVs, dzData = dzData, mzData = mzData)
m1 = umxRun(m1)
umxSummaryACE(m1)

# ===============================
# = Drop c and test loss of fit =
# ===============================
omxGetParameters(m1, free = T) # nb: constructors label all cells as "matrix_rNcN" i.e., "a_r1c1"
# So to drop c, in this case we fix cell "c_r1c1" at zero
m2 = umxReRun(m1, dropList = "c_r1c1", name = "dropC")
umxCompare(m1, m2)
umxSummaryACE(m2, comparison = m1)

# =====================================
# = Example with covariance data only =
# =====================================
m1 = umxACE(selDVs = selDVs, dzData = cov(dzData, use = "pairwise"), mzData = cov(mzData, use = "pairwise"), numObsDZ = nrow(dzData), numObsMZ = nrow(mzData))
m1 = umxRun(m1)
umxSummaryACE(m1)
m2 = umxReRun(m1, dropList = "c_r1c1", name = "dropC")
umxSummaryACE(m2, comparison = m1)

# And make a Figure in dot format!
# If you have installed GraphViz, the next command will open it for you to see!
\dontrun{
	umxPlotACE(m2)
}
}

