% Generated by roxygen2 (4.0.0.99): do not edit by hand
\name{coef.MxModel}
\alias{coef.MxModel}
\title{coef.MxModel}
\usage{
\method{coef}{MxModel}(model)
}
\arguments{
\item{model}{an \code{\link{mxModel}} to get the AIC from}
}
\value{
- coefficients
}
\description{
Returns the coeficients from an OpenMx RAM model
}
\examples{
require(OpenMx)
data(demoOneFactor)
latents  = c("G")
manifests = names(demoOneFactor)
m1 <- mxModel("One Factor", type = "RAM",
	manifestVars = manifests, latentVars = latents,
	mxPath(from = latents, to = manifests),
	mxPath(from = manifests, arrows = 2),
	mxPath(from = latents, arrows = 2, free = FALSE, values = 1.0),
	mxData(cov(demoOneFactor), type = "cov", numObs = 500)
)
m1 = umxRun(m1, setLabels = TRUE, setValues = TRUE)
coef(m1)
# -2.615998
}
\references{
-
}
\seealso{
- \code{\link{AIC}}, \code{\link{umxCompare}}, \code{\link{mxStandardizeRAMpaths}}
}

