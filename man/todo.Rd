% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/todo.R
\name{todo}
\alias{todo}
\title{Visualizer and Manager for your Markdown To-Do List}
\usage{
todo(
  file = "todo.md",
  html = "todo.html",
  do = c("show", "open"),
  where = NULL,
  browser = TRUE
)
}
\arguments{
\item{file}{The filename of the markdown to-do list (defaults to \code{"todo.md"}).}

\item{html}{The filename of the output HTML file (defaults to \code{"todo.html"}).}

\item{do}{Character. Whether to \code{"show"} (render HTML and open in browser) or \code{"open"} (open raw markdown file in default editor).}

\item{where}{Optional path to a directory. If specified, saves this directory as your default fallback location for todo files.}

\item{browser}{Logical. If \code{TRUE} (default), opens the HTML in browser (only applicable when \code{do = "show"}).}
}
\value{
The resolved path to the HTML or Markdown file (invisibly).
}
\description{
\code{todo} reads a Markdown to-do file, renders it with a gorgeous dark glassmorphic UI
(with live clock and calendar widget), and opens it in your browser (\code{do = "show"}).
Alternatively, it can open the raw markdown file directly in your default system markdown editor (\code{do = "open"}).

By default, it looks for \code{todo.md} in the current working directory. If none is found,
it checks for a saved default directory (set via the \code{where} parameter). If that is also absent,
it initializes a new \code{todo.md} in the current working directory with a breakthrough checklist template.
}
\examples{
\dontrun{
# Render and show in browser
todo()

# Open raw markdown file in default editor
todo(do = "open")

# Save a global default directory and use it
todo(where = "~/Documents/todo")
}
}
\seealso{
\itemize{
\item \code{\link[=umx]{umx()}}
}
}
\concept{Utility functions}
