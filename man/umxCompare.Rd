% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/umxCompare.R
\name{umxCompare}
\alias{umxCompare}
\title{Print a comparison table of two or more \code{\link[OpenMx:mxModel]{OpenMx::mxModel()}}s, nicely formatted.}
\usage{
umxCompare(
  base = NULL,
  comparison = NULL,
  all = TRUE,
  digits = 3,
  report = c("markdown", "html", "inline"),
  compareWeightedAIC = FALSE,
  silent = FALSE,
  file = "tmp.html",
  uncertainty = c("none", "SE", "RobustSE", "CI")
)
}
\arguments{
\item{base}{The base \code{\link[OpenMx:mxModel]{OpenMx::mxModel()}} for comparison}

\item{comparison}{The model (or list of models) which will be compared for fit with the base model (can be empty)}

\item{all}{Whether to make all possible comparisons if there is more than one base model (defaults to T)}

\item{digits}{rounding for p-values etc.}

\item{report}{"markdown" (default), "inline" (a sentence suitable for inclusion in a paper), or "html".
create a web table and open your default browser.
(handy for getting tables into Word, and other text systems!)}

\item{compareWeightedAIC}{Show the Wagenmakers AIC weighted comparison (default = FALSE)}

\item{silent}{(don't print, just return the table as a dataframe (default = FALSE)}

\item{file}{file to write html too if report = "html" (defaults to "tmp.html")}

\item{uncertainty}{What type of parameter uncertainty to report: "SE" (standard ML standard errors), "RobustSE" (robust standard errors), "CI" (profile likelihood confidence intervals), or "none" (none).}
}
\description{
umxCompare compares two or more \code{\link[OpenMx:mxModel]{OpenMx::mxModel()}}s. It has several nice features:
\enumerate{
\item Direct control of precision via digits=. p-values use APA style.
\item Report = directs publication to your chosen format: markdown, html, latex.
\item Columns arranged to make for easy visual comparison.
\item report = 'inline', includes a summary sentence suitable for your report.
\item report = "html" opens a web table in your browser to paste into a word processor.
}

\strong{Interpreting fit under WLS/DWLS and Genomic SEM}

Hu & Bentler (1999) style cutoffs (e.g. CFI > .95, RMSEA < .06) were developed for
maximum likelihood with continuous data. They do \strong{not} transfer to WLS/DWLS
(Shi et al., 2020), and still less to Genomic SEM (DWLS on LDSC genetic
covariances with an estimated sampling covariance, often with bookkeeping \code{numObs = 1}).
\itemize{
\item \strong{Incremental indices (CFI, TLI, NFI, …)} compare the model to an independence
baseline under the same weight matrix. That baseline behaves differently than under ML.
Report CFI/TLI only \strong{descriptively}; do \strong{not} apply conventional cutoffs.
\item \strong{Absolute fit: SRMR} (and residual matrices) is preferred. SRMR lives on a
standardized residual metric and is less hostage to weight-matrix / N scaling.
Rough guide: SRMR < 0.10 suggests acceptable residual structure (not a hard law).
\item \strong{RMSEA} folds in chi-square scaling and N; it is often misleading for WLS and
especially for GSEM. Do not use the ML cutoff of < .06.
\item \strong{Nested model building:} prefer scaled chi-square difference tests and ΔSRMR
over ΔCFI alone (see Details for what \code{umxCompare} computes).
}
}
\details{
\strong{What \code{umxCompare} does for WLS}
\itemize{
\item Refuses mixed WLS vs ML comparisons (same engine required).
\item \strong{Continuous WLS} with cached \code{implied_jacobian}:
\itemize{
\item Table \strong{Chi} is the same Satorra–Bentler (2010) scaled omnibus as \code{\link[=umxSummary]{umxSummary()}}
(\eqn{F/c} from \code{output$fit}), \strong{not} OpenMx Browne residual \code{output$chi}
(those two differ under DWLS). Saturated residual df reports Chi = 0, CFI = 1, RMSEA = 0.
\item Nested \strong{diffFit} is SB-2010 scaled \(\Delta F = F_{\mathrm{nested}} - F_{\mathrm{base}}\)
(requires both models at comparable minima of the same WLS objective: same moments,
\code{useWeight}, and \(N\)). If \(F_{\mathrm{nested}} < F_{\mathrm{base}}\) (often multigroup
optimization), diffFit is \strong{NA} with a warning — not a negative χ² with p = 1.
}
\item \strong{Genomic SEM} (\code{MxModelGSEM}): nested difference on the \strong{GSEM DWLS chi-square
scale} (same discrepancy as the structural fit; not a substitute for ML LRT).
If LDSC matrices were \code{nearPD}-smoothed, a fiduciary warning notes that difference
tests may look artificially precise.
\item Table also reports \strong{SRMR / ΔSRMR} (preferred absolute residual summary) and
\strong{CFI / ΔCFI} (descriptive only). AIC may appear but is not a primary GSEM decision
rule under asymptotic / bookkeeping N.
}

\strong{Ordinal WLS} is a separate track: robust CFI/TLI/RMSEA use Savalei (2021) catML
corrections in \code{\link[=umxSummary]{umxSummary()}}; Hu–Bentler cutoffs can apply to \strong{those} robust indices.
That exception does \strong{not} apply to continuous WLS or Genomic SEM.

Best practice: report estimates with SEs, SRMR (and residuals), nested SB/GSEM
difference tests, and avoid single-number “good fit” claims from CFI or RMSEA.
}
\examples{
\dontrun{
require(umx)
data(demoOneFactor)
manifests = names(demoOneFactor)

m1 = umxRAM("One Factor", data = demoOneFactor, type = "cov",
	umxPath("G", to = manifests),
	umxPath(var = manifests),
	umxPath(var = "G", fixedAt = 1)
)

m2 = umxModify(m1, update = "G_to_x2", name = "drop_path_2_x2")
umxCompare(m1, m2)
umxCompare(m1, m2, report = "inline") # Add English-sentence descriptions
umxCompare(m1, m2, report = "html") # Open table in browser

# Two comparison models
m3 = umxModify(m2, update = "G_to_x3", name = "drop_path_2_x2_and_3")

umxCompare(m1, c(m2, m3))
umxCompare(m1, c(m2, m3), compareWeightedAIC = TRUE)
umxCompare(c(m1, m2), c(m2, m3), all = TRUE)

manifests = names(demoOneFactor)
m1 = umxRAM("WLS", data = demoOneFactor, type = "DWLS",
	umxPath("G", to = manifests),
	umxPath(var = manifests),
	umxPath(var = "G", fixedAt = 1)
)

m2 = umxModify(m1, update = "G_to_x2", name = "drop_path_2_x2")
umxCompare(m1, m2)
umxCompare(m1, m2, report = "inline") # Add English-sentence descriptions
umxCompare(m1, m2, report = "html") # Open table in browser
}
}
\references{
\itemize{
\item \url{https://github.com/tbates/umx}
}
}
\seealso{
\itemize{
\item \code{\link[=umxSummary]{umxSummary()}}, \code{\link[=umxRAM]{umxRAM()}},\code{\link[=umxCompare]{umxCompare()}}
}

Other Model Summary and Comparison:
\code{\link{umx}},
\code{\link[=umxEquate]{umxEquate()}},
\code{\link[=umxMI]{umxMI()}},
\code{\link[=umxReduce]{umxReduce()}},
\code{\link[=umxSetParameters]{umxSetParameters()}},
\code{\link[=umxSummary]{umxSummary()}},
\code{\link[=xmu_compare_WLS]{xmu_compare_WLS()}},
\code{\link[=xmu_robust_WLS_fit]{xmu_robust_WLS_fit()}}
}
\concept{Model Summary and Comparison}
