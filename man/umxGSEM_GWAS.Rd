% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/umxGSEM.R
\name{umxGSEM_GWAS}
\alias{umxGSEM_GWAS}
\title{Multivariate genomic SEM GWAS (SNP on a factor or user model)}
\usage{
umxGSEM_GWAS(
  covstruc,
  SNPs,
  model = NULL,
  estimation = c("DWLS", "WLS", "ULS"),
  traits = NULL,
  GC = c("standard", "conserv", "none"),
  uncertainty = c("robustSE", "SE"),
  SnpSamplingError = 5e-04,
  maxSNPs = NULL,
  snpEffect = "SNP_to_F1",
  quiet = TRUE,
  fix_measurement = TRUE,
  force_fallback = FALSE
)
}
\arguments{
\item{covstruc}{LDSC list with \code{S} (genetic covariance), \code{V} (sampling covariance of
vech(S)), and preferably \code{I} (LDSC intercept matrix, for GC-corrected SEs).
See e.g. \link{Psych_LDSC} (\code{I} = intercepts / sample-overlap; also \code{N}, \code{m}).}

\item{SNPs}{Data frame from \code{\link[=umxGSEM_sumstats]{umxGSEM_sumstats()}} (columns \verb{beta.*}, \verb{se.*}, \code{MAF}, \code{SNP}, ...).}

\item{model}{Optional lavaan/umx string or mxModel. If \code{NULL}, builds
A RAM model with one latent (F1) loading on all the traits, and  "SNP" loading on F1}

\item{estimation}{\code{"DWLS"} (default), \code{"WLS"}, or \code{"ULS"}.}

\item{traits}{Trait names. Default: colnames of \code{covstruc$S}.}

\item{GC}{Genomic control for SNP sampling variances: \code{"standard"}, \code{"conserv"}, or \code{"none"}.}

\item{uncertainty}{Whether to compute the robust sandwich SE (\code{"robustSE"}, default) or naive SE (\code{"SE"}).}

\item{SnpSamplingError}{SE used for SNP variance (treated as nearly fixed; default 5e-4).}

\item{maxSNPs}{Optional limit for smoke tests.}

\item{snpEffect}{Character; path label to extract from the model (default \code{"SNP_to_F1"}).}

\item{quiet}{Suppress per-SNP messages.}

\item{fix_measurement}{Logical; if TRUE (default), fixes the non-SNP measurement model parameters to their base optimized values when evaluating each SNP (only applies if analytic engine is bypassed).}

\item{force_fallback}{Logical; internal use for testing to force the non-vectorized analytic path (default \code{FALSE}).}
}
\value{
A data.frame of SNP results (\code{SNP}, \code{CHR}, \code{BP}, \code{MAF}, \code{est}, \code{se}, \code{Z}, \code{P}, \code{status}, ...).
}
\description{
Expands the LDSC genetic covariance structure with each SNP (using prepared
betas from \code{\link[=umxGSEM_sumstats]{umxGSEM_sumstats()}}), fits DWLS/WLS, and returns SNP-level results.
For a default common-factor model, estimates \code{F1 ~ SNP}.
}
\details{
The LDSC object is the usual GenomicSEM-style list (\code{S}, \code{V}, preferably \code{I});
see e.g. \link{Psych_LDSC} for a packaged example and field definitions.
}
\examples{
\dontrun{
1. load data
data(Psych_LDSC)
# Have a quick look if you like
# Psych_LDSC fields: S, V, I, N, m (see ?Psych_LDSC). V pairs are named var_SCZ, poly_BIP_SCZ, ...
umxSummary(Psych_LDSC)

#' # 2. build the snp data structure from your SNP data txt files, e.g.
dir = "~/bin/umx/inst/developer/GenomicSEM"
snps = umxGSEM_sumstats(
  files = file.path(dir, c("SCZ_subset.txt", "BIP_subset.txt", "MDD_subset.txt")),
  ref = file.path(dir, "reference.1000G.subset.txt"),
  trait.names = c("SCZ", "BIP", "MDD"), se.logit = TRUE
)

# 3. Run the GWAS on your trait measurement model adding SNP effects into the latent structure
GWAS = umxGSEM_GWAS(covstruc = Psych_LDSC, SNPs = snps,
  traits = c("SCZ", "BIP", "MDD"), maxSNPs = 20)

# 4. Examine results and make a new drug or something useful!
head(GWAS)
# Manhattan Plot (default)
plot(gwas_res)
plot(example_gwas, chromosomes= 1, label_top_n= 4, title="IQ GWAS") + theme_plos()
volcano_plot(example_gwas, size_by = "AF", label_top_n = 3)

# Q-Q Plot
plot(gwas_res, type = "qq")

# ================
# = Another demo =
# ================
# 1. Load the reference LDSC covariance structure
data(Psych_LDSC, package = "umx")

# 2. Generate a million simulated SNPs
traits = c("SCZ", "BIP", "MDD")
t0 = Sys.time()
snps = umxGSEM_sim_snps(n = 1e6, traits = traits)
print(Sys.time() - t0)

# 3. Run the GWAS and time it
t0 = Sys.time()
gwas_res = umxGSEM_GWAS(covstruc = Psych_LDSC, SNPs = snps, traits = traits)
print(Sys.time() - t0)

# 4. View the results
head(gwas_res)
# Manhattan Plot (default)
plot(gwas_res)
plot(example_gwas, chromosomes= 1, label_top_n= 4, title="IQ GWAS") + theme_plos()
volcano_plot(example_gwas, size_by = "AF", label_top_n = 3)

# Q-Q Plot
plot(gwas_res, type = "qq")

}
}
\seealso{
\code{\link[=umxGSEM_sumstats]{umxGSEM_sumstats()}}, \code{\link[=umxGSEM]{umxGSEM()}}, \link{Psych_LDSC}

Other GSEM:
\code{\link{Anthro_LDSC}},
\code{\link{Psych_LDSC}},
\code{\link[=plot.umx_GWAS]{plot.umx_GWAS()}},
\code{\link[=umxGSEM]{umxGSEM()}},
\code{\link[=umxGSEM_dl_RefList]{umxGSEM_dl_RefList()}},
\code{\link[=umxGSEM_label_ldsc]{umxGSEM_label_ldsc()}},
\code{\link[=umxGSEM_ldsc]{umxGSEM_ldsc()}},
\code{\link[=umxGSEM_munge]{umxGSEM_munge()}},
\code{\link[=umxGSEM_std]{umxGSEM_std()}},
\code{\link[=umxGSEM_sumstats]{umxGSEM_sumstats()}},
\code{\link[=umxSummary.MxDataStatic]{umxSummary.MxDataStatic()}},
\code{\link[=umxSummary.list]{umxSummary.list()}}
}
\concept{GSEM}
