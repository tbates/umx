% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/umxGSEM.R
\name{umxGSEM_dl_tutorial_files}
\alias{umxGSEM_dl_tutorial_files}
\title{Download tutorial-scale Genomic SEM files}
\usage{
umxGSEM_dl_tutorial_files(
  path = NULL,
  overwrite = FALSE,
  url =
    "https://github.com/tbates/umx/releases/download/gsem-tutorial/umxGSEM_tutorial.tar.gz"
)
}
\arguments{
\item{path}{Directory to write into. Default:
\code{file.path(tools::R_user_dir("umx", "data"), "GSEM_tutorial")}.}

\item{overwrite}{If \code{TRUE}, re-download the Release tarball even when files
already exist. Default \code{FALSE}.}

\item{url}{Tarball URL, or a path to a local \code{.tar.gz}. Default is the
\code{gsem-tutorial} Release asset on \code{tbates/umx}.}
}
\value{
A named list of absolute paths: \code{dir}, \code{hm3}, \code{ld}, \code{sumstats}
(character vector of three subset files), \code{ref}.
}
\description{
Fetches the IBG / GenomicSEM practical pack (~45 MB): 1k-SNP SCZ/BIP/MDD
sumstats, a tiny 1000G MAF table, HapMap3 \code{w_hm3.snplist}, and EUR LD scores
(\verb{eur_w_ld_chr/}). That is enough to run \code{\link[=umxGSEM_munge]{umxGSEM_munge()}} and see ldsc on a
toy SNP list. It is \strong{not} a production data installer.

Files land in \code{tools::R_user_dir("umx", "data")/GSEM_tutorial} unless \code{path}
is set. 1k-SNP tables and LD scores come from the umx GitHub Release tag
\code{gsem-tutorial} (not from the CRAN tarball). Existing files are left alone
unless \code{overwrite = TRUE}.

\strong{ldsc on these SNPs is pedagogical.} About 1k variants will not recover
sensible heritabilities. For a real structural model use \link{Psych_LDSC} /
\link{Anthro_LDSC} (or a covstruc from full-sumstat ldsc).

\strong{Production files (do not use this helper):}
\itemize{
\item Full GWAS sumstats from the relevant consortium (not "top hits").
\item HapMap3 list: \verb{https://zenodo.org/records/7773502/files/w_hm3.snplist.gz}
(gunzip to \code{w_hm3.snplist}; header must include \code{SNP}).
\item Local ldsc only: Alkes EUR LD scores
\verb{https://alkesgroup.broadinstitute.org/LDSCORE/eur_w_ld_chr.tar.bz2}
(~500 MB -> ~1.2 GB) and \code{weights_hm3_noMHC.tgz} (~150 MB). Ancestry must
match the GWAS. Skip if you already have a covstruc.
\item 1000G EUR plink (~10-20 GB) only if sumstats lack allele frequency and you
need an FRQ filter in munge. GenomicSEM's 1000G MAF table is the usual GSEM
path, not bim/bed/bam.
}
}
\examples{
\dontrun{
tut = umxGSEM_dl_tutorial_files()
umxGSEM_munge(tut$sumstats, hm3 = tut$hm3, trait.names = c("SCZ", "BIP", "MDD"),
	Ns = c(105318, 16731, 173005), output_dir = tut$dir)
data(Psych_LDSC)
m1 = umxGSEM("g ~= SCZ + BIP + MDD", covstruc = Psych_LDSC)
}
}
\seealso{
\code{\link[=umxGSEM_munge]{umxGSEM_munge()}}, \code{\link[=umxGSEM]{umxGSEM()}}, \code{\link[=umxGSEM_GWAS]{umxGSEM_GWAS()}}, \link{Psych_LDSC}

Other GSEM:
\code{\link{Anthro_LDSC}},
\code{\link{Psych_LDSC}},
\code{\link[=plot.umx_GWAS]{plot.umx_GWAS()}},
\code{\link[=umxGSEM]{umxGSEM()}},
\code{\link[=umxGSEM_GWAS]{umxGSEM_GWAS()}},
\code{\link[=umxGSEM_label_ldsc]{umxGSEM_label_ldsc()}},
\code{\link[=umxGSEM_ldsc]{umxGSEM_ldsc()}},
\code{\link[=umxGSEM_munge]{umxGSEM_munge()}},
\code{\link[=umxGSEM_std]{umxGSEM_std()}},
\code{\link[=umxGSEM_sumstats]{umxGSEM_sumstats()}},
\code{\link[=umxSummary.MxDataStatic]{umxSummary.MxDataStatic()}},
\code{\link[=umxSummary.list]{umxSummary.list()}}
}
\concept{GSEM}
