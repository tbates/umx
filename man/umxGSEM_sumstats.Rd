% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/umxGSEM.R
\name{umxGSEM_sumstats}
\alias{umxGSEM_sumstats}
\title{Prepare multivariate SNP summary statistics for genomic SEM GWAS}
\usage{
umxGSEM_sumstats(
  files,
  ref,
  trait.names = NULL,
  se.logit = TRUE,
  OLS = FALSE,
  linprob = FALSE,
  N = NULL,
  info.filter = 0.6,
  maf.filter = 0.01
)
}
\arguments{
\item{files}{Character vector of GWAS summary statistic paths (same trait order as LDSC).}

\item{ref}{Path to 1000G reference with columns SNP, CHR, BP, MAF, A1, A2.}

\item{trait.names}{Trait names (defaults to file basenames).}

\item{se.logit}{Logical vector (one per trait): SE is on the logistic scale (default TRUE for case/control).}

\item{OLS}{Logical vector: continuous OLS traits.}

\item{linprob}{Logical vector: back out logistic betas from Z and N.}

\item{N}{Optional sample sizes (total for OLS; sum of effective N for binary linprob).}

\item{info.filter}{INFO threshold (default 0.6).}

\item{maf.filter}{MAF threshold applied to the reference (default 0.01).}
}
\value{
A data.frame with SNP, CHR, BP, MAF, A1, A2, and \verb{beta.*} / \verb{se.*} per trait.
}
\description{
Aligns per-trait GWAS files to a 1000 Genomes reference (allele match + MAF),
converts ORs to log-scale when needed, and applies the continuous-scale
transformation used in Genomic SEM (\code{beta} / \code{se} relative to unit-variance
phenotypes). Output is ready for \code{\link[=umxGSEM_GWAS]{umxGSEM_GWAS()}}.
}
\examples{
\dontrun{
dir = system.file("developer", "GenomicSEM", package = "umx")
# or local: dir = "~/bin/umx/inst/developer/GenomicSEM"
files = file.path(dir, c("SCZ_subset.txt", "BIP_subset.txt", "MDD_subset.txt"))
ref   = file.path(dir, "reference.1000G.subset.txt")
snps  = umxGSEM_sumstats(files, ref, trait.names = c("SCZ", "BIP", "MDD"), se.logit = TRUE)
head(snps)
}
}
\seealso{
\code{\link[=umxGSEM_GWAS]{umxGSEM_GWAS()}}, \code{\link[=umxGSEM]{umxGSEM()}}

Other Genomic SEM Functions:
\code{\link[=umxGSEM]{umxGSEM()}},
\code{\link[=umxGSEM_GWAS]{umxGSEM_GWAS()}},
\code{\link[=umxGSEM_dl_RefList]{umxGSEM_dl_RefList()}},
\code{\link[=umxGSEM_munge]{umxGSEM_munge()}}
}
\concept{Genomic SEM Functions}
