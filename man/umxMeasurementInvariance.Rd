% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/umxMeasurementInvariance.R
\name{umxMeasurementInvariance}
\alias{umxMeasurementInvariance}
\title{Test measurement invariance of a confirmatory factor analysis model}
\usage{
umxMeasurementInvariance(
  model,
  data = NULL,
  group = NULL,
  type = "all",
  compare = c("both", "sequential", "configural"),
  silent = FALSE,
  ...
)
}
\arguments{
\item{model}{A lavaan syntax string defining the CFA model.}

\item{data}{A data frame containing the data.}

\item{group}{Character string, the name of the grouping variable.}

\item{type}{Character vector, which tests to run. Default is "all", which runs configural, weak, strong, and strict models.}

\item{compare}{Character string, how to compare the models: "both", "sequential", or "configural". Default is "both".}

\item{silent}{Boolean, whether to suppress output and messages. Default is FALSE.}

\item{...}{Additional arguments passed to \code{\link[=umxRAM]{umxRAM()}}.}
}
\value{
A model comparison table (data frame). If \code{compare} is "both" (default) or "sequential", the sequential comparison table is returned, and the configural/baseline comparison table is attached as the \code{configural} attribute. If \code{compare} is "configural", the baseline comparison table is returned.
}
\description{
\code{umxMeasurementInvariance} runs a sequence of multi-group CFA models
to test for configural, weak (metric), strong (scalar), and strict invariance
across groups.
}
\examples{
\dontrun{
data("HS.ability.data", package = "OpenMx")
df = umx_scale(HS.ability.data[, c("visual", "cubes", "flags", "school")])
model = "spatial =~ visual + cubes + flags"
umxMeasurementInvariance(model, data = df, group = "school")
}
}
\seealso{
\itemize{
\item \code{\link[=umxRAM]{umxRAM()}}, \code{\link[=umxCompare]{umxCompare()}}
}
}
\concept{Model Comparison}
