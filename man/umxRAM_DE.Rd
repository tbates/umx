% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/umxRAM_DE.R
\name{umxRAM_DE}
\alias{umxRAM_DE}
\title{Build and run path-based Double-entry SEM models including censored data}
\usage{
umxRAM_DE(
  model = NA,
  ...,
  data = NULL,
  DEvars = NULL,
  doubleEntrySuffix = c("_cont", "_cens"),
  fixCensorThresholds = c("yes", "auto", "no"),
  censorCuts = NULL,
  sep = NULL,
  name = NA,
  group = NULL,
  group.equal = NULL,
  suffix = "",
  comparison = TRUE,
  type = c("Auto", "FIML"),
  weight = NULL,
  allContinuousMethod = c("cumulants", "marginals"),
  autoRun = getOption("umx_auto_run"),
  tryHard = c("no", "yes", "ordinal", "search"),
  std = FALSE,
  refModels = NULL,
  remove_unused_manifests = TRUE,
  independent = NA,
  setValues = TRUE,
  optimizer = NULL,
  verbose = FALSE
)
}
\arguments{
\item{model}{A model to update (or set to string to use as name for new model)}

\item{...}{umxPaths, mxThreshold objects, etc.}

\item{data}{data for the model. Can be an \code{\link[OpenMx:mxData]{OpenMx::mxData()}} or a data.frame}

\item{DEvars}{A character vector of base names for double-entry variables (e.g. \code{c("litres")} creates \code{litres_cont}/\code{litres_cens}). Base names only; do not include suffix.}

\item{doubleEntrySuffix}{Suffixes for the continuous and censored variables (default = c("_cont", "_cens")).}

\item{fixCensorThresholds}{One of \code{c("yes","auto","no")}. \code{"yes"} fix every DE pair from \code{censorCuts}/prep attr; \code{"auto"} fix only pairs with finite known cut; \code{"no"} free thresholds.}

\item{censorCuts}{Optional named numeric vector of known cuts on analysis scale. Names may be base (\code{"litres"}), \code{"_cont"} or \code{"_cens"} form.}

\item{sep}{Separator used in prep (default \code{NULL} infers from \code{attr(data,"umxDoubleEntry")$sep} or \code{""}).}

\item{name}{A friendly name for the model.}

\item{group}{(optional) Column name to use for a multi-group model (default = NULL). Fixed DE thresholds (known cut) are applied in every group.}

\item{group.equal}{In multi-group models, what to equate across groups (default = NULL: all free)}

\item{suffix}{String to append to each label (useful if model will be used in a multi-group model)}

\item{comparison}{Compare the new model to the old (if updating an existing model: default = TRUE)}

\item{type}{One of \code{"Auto"} or \code{"FIML"}. \code{"WLS"}, \code{"DWLS"}, \code{"ULS"}, \code{"cov"}, and \code{"cor"} are rejected (use \code{\link[=umxRAM]{umxRAM()}}).}

\item{weight}{Passes weight values to mxData}

\item{allContinuousMethod}{"cumulants" or "marginals". Used in all-continuous WLS data to determine if a means model needed.}

\item{autoRun}{Whether to run the model (default), or just to create it and return without running.}

\item{tryHard}{Default ('no') uses normal mxRun. "yes" uses mxTryHard. Other options: "ordinal", "search"}

\item{std}{Whether to show standardized estimates, raw (NULL print fit only)}

\item{refModels}{pass in reference models if available. Use FALSE to suppress computing these if not provided.}

\item{remove_unused_manifests}{Whether to remove variables in the data to which no path makes reference (defaults to TRUE)}

\item{independent}{Whether the model is independent (default = NA)}

\item{setValues}{Whether to generate likely good start values (Defaults to TRUE)}

\item{optimizer}{optionally set the optimizer (default NULL does nothing)}

\item{verbose}{Whether to tell the user what latents and manifests were created etc. (Default = FALSE)}
}
\value{
\itemize{
\item \code{\link[OpenMx:mxModel]{OpenMx::mxModel()}}
}
}
\description{
\code{umxRAM_DE} expedites SEM with censored data. Each name in \code{DEvars} is a \strong{latent} trait.
Prepared \verb{_cont} / \verb{_cens} columns are indicators of that latent (loading fixed at 1, residual fixed at 0).
Write ordinary \code{\link[=umxPath]{umxPath()}} calls using the base name (e.g. \code{"litres"}). Do not Cartesian-expand to \verb{_cont}/\verb{_cens}.

Fully continuous variables may be mixed with double-entry pairs. Prepare the censored traits with
\code{\link[=umx_make_double_entry_data]{umx_make_double_entry_data()}}. At least one \code{DEvar} is required.

As you can see from the examples below, most of the work is done by \code{\link[=umxPath]{umxPath()}}. \code{umxRAM_DE} wraps these paths up, takes the \verb{data =} input, and
then builds the latent measurement model for each DE trait and sets thresholds. By default it will also run it.

\strong{Gotchas}
A common error is to include data in the main list, a bit like
saying \code{lm(y ~ x + df)} instead of \code{lm(y ~ x, data = df)}.

\strong{nb}: Because it uses the presence of a variable in the data to detect if a variable is latent or not, \code{umxRAM} needs data at build time.

If you are used to hacking a quick model with \link[=umxLav2RAM]{lavaan string syntax}, use \code{\link[=umxRAM]{umxRAM()}} at present. Likewise, if you are at the "sketching" stage of theory consideration, \code{umxRAM} supports that, umxRAM_DE does not.
}
\details{
\strong{WLS / cov / cor}
\code{type = "WLS"}, \code{"DWLS"}, \code{"ULS"}, \code{"cov"}, or \code{"cor"} is not valid in \code{umxRAM_DE}.
Double-entry needs raw-data FIML (each row is either the continuous density or the
threshold CDF). Those types fit a correlation/moment structure and cannot use the
mutual-NA \verb{_cont}/\verb{_cens} pattern. For WLS/cov/cor on ordinary (non-DE) data, use \code{\link[=umxRAM]{umxRAM()}}.
}
\examples{
# Here's a path example that models miles per gallon (mpg) as a function of weight (wt) and 
# engine displacement (disp) using the widely used `mtcars` data set.

# 1: Create the censored data
# Just for a demo, we will create a censored litres column, censoring at 2.
data(mtcars)
tmp = mtcars
tmp$litres = tmp$disp / 61.02
tmp = umx_make_double_entry_data(tmp, cols = list(litres= 2), sep="", nSib=1)
# 2: Create the RAM_DE model
m1 = umxRAM_DE("test", data = tmp, DEvars = c("litres"),
	umxPath("litres", to = "mpg"),
	umxPath("wt", to = "mpg"),
	umxPath("wt", with = "litres"),
	umxPath(v.m. = c("litres", "wt", "mpg"))
)

}
\references{
\itemize{
\item \url{https://tbates.github.io}, \url{https://github.com/tbates/umx}
}
}
\seealso{
\code{\link[=umxPath]{umxPath()}}, \code{\link[=umxSummary]{umxSummary()}}, \code{\link[=plot]{plot()}}, \code{\link[=parameters]{parameters()}}, \code{\link[=umxSuperModel]{umxSuperModel()}}, \code{\link[=umxLav2RAM]{umxLav2RAM()}}

Other Core Model Building Functions:
\code{\link{umx}},
\code{\link[=umxLISREL]{umxLISREL()}},
\code{\link[=umxMatrix]{umxMatrix()}},
\code{\link[=umxModify]{umxModify()}},
\code{\link[=umxPath]{umxPath()}},
\code{\link[=umxRAM]{umxRAM()}},
\code{\link[=umxSuperModel]{umxSuperModel()}}
}
\concept{Core Model Building Functions}
