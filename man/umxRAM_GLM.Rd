% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/umxRAM_GLM.R
\name{umxRAM_GLM}
\alias{umxRAM_GLM}
\title{Path-based RAM model with mxFitFunctionGLM}
\usage{
umxRAM_GLM(
  model = NA,
  ...,
  data = NULL,
  families = NULL,
  theta = NULL,
  nAGQ = NA_integer_,
  name = NA,
  autoRun = getOption("umx_auto_run"),
  tryHard = c("no", "yes", "ordinal", "search"),
  setValues = TRUE,
  suffix = "",
  verbose = FALSE
)
}
\arguments{
\item{model}{a model name (character). Updating an existing model is not supported.}

\item{...}{\code{\link[=umxPath]{umxPath()}} / \code{\link[OpenMx:mxPath]{mxPath()}} objects, optional \code{\link[OpenMx:mxMatrix]{mxMatrix()}}, \code{\link[OpenMx:mxConstraint]{mxConstraint()}}, and \code{\link[OpenMx:mxFamily]{mxFamily()}} objects. Not data.}

\item{data}{a data.frame or raw \code{\link[OpenMx:mxData]{mxData()}}. Required (used to detect manifests vs latents and to set starts).}

\item{families}{named list of \code{stats::family} or \code{\link[OpenMx:mxFamily]{mxFamily()}} objects. Names are manifest variables. Items not listed are Gaussian.}

\item{theta}{optional named numeric (fixed) or character (free-parameter label) for NB size / Gamma shape / inverse-Gaussian lambda.}

\item{nAGQ}{passed to \code{\link[OpenMx:mxFitFunctionGLM]{mxFitFunctionGLM()}}. \code{NA} (default) uses OpenMx's rule: 5 for binomial with one latent, else 1.}

\item{name}{optional name; if \code{NA}, \code{model} is used.}

\item{autoRun}{whether to run (default \code{\link[=umx_set_auto_run]{umx_set_auto_run()}}).}

\item{tryHard}{\code{\link[=xmu_safe_run_summary]{xmu_safe_run_summary()}} tryHard options.}

\item{setValues}{if TRUE, set start values (means by family; Gaussian residual variances from the data; GLM-item \code{S} at 0).}

\item{suffix}{passed to \code{\link[=xmuLabel]{xmuLabel()}}.}

\item{verbose}{currently unused; kept for umxRAM-like calling.}
}
\value{
an \code{\link[OpenMx:MxModel]{MxModel()}} (invisibly if autoRun prints a summary)
}
\description{
Standalone counterpart to \code{\link[=umxRAM]{umxRAM()}} for conditional exponential-family
manifests. Latents are anything in the paths that is not in \code{data}. Manifests
are anything in the paths (or in \code{families}) that is in \code{data}. Residual \code{S}
on family items is fixed at 0. Manifests not listed in \code{families} are
Gaussian (free residual \code{S}). Does not call \code{\link[OpenMx:mxRefModels]{mxRefModels()}} (no CFI yet).
}
\examples{
\dontrun{
set.seed(1)
dat = data.frame(y = rpois(80, lambda = exp(0.3)))
m1 = umxRAM_GLM("poi",
	umxPath("one", to = "y"),
	data = dat,
	families = list(y = poisson()),
	autoRun = TRUE)
}
}
\seealso{
\code{\link[=umxRAM]{umxRAM()}}, \code{\link[OpenMx:mxFamily]{mxFamily()}}, \code{\link[OpenMx:mxFitFunctionGLM]{mxFitFunctionGLM()}}

Other Advanced Model Building Functions:
\code{\link{umx}},
\code{\link[=umxAlgebra]{umxAlgebra()}},
\code{\link[=umxFixAll]{umxFixAll()}},
\code{\link[=umxJiggle]{umxJiggle()}},
\code{\link[=umxRun]{umxRun()}},
\code{\link[=umxThresholdMatrix]{umxThresholdMatrix()}},
\code{\link[=umxUnexplainedCausalNexus]{umxUnexplainedCausalNexus()}},
\code{\link[=xmuLabel]{xmuLabel()}},
\code{\link[=xmuValues]{xmuValues()}}
}
\concept{Advanced Model Building Functions}
