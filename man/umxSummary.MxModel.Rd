% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/umxSummary.MxModel.R
\name{umxSummary.MxModel}
\alias{umxSummary.MxModel}
\alias{umxSummary.MxRAMModel}
\title{Shows a compact, publication-style, summary of a RAM model}
\usage{
\method{umxSummary}{MxModel}(
  model,
  refModels = NULL,
  std = FALSE,
  digits = 2,
  report = c("markdown", "html"),
  means = TRUE,
  residuals = TRUE,
  uncertainty = c("SE", "RobustSE", "CI", "none"),
  filter = c("ALL", "NS", "SIG"),
  RMSEA_CI = FALSE,
  SE = TRUE,
  matrixAddresses = FALSE,
  ...
)
}
\arguments{
\item{model}{The \code{\link[OpenMx:mxModel]{OpenMx::mxModel()}} whose fit will be reported}

\item{refModels}{Saturated models if needed for fit indices (see example below:
If NULL will be computed on demand. If FALSE will not be computed.}

\item{std}{If TRUE, model is standardized (Default FALSE, NULL means "don't show").}

\item{digits}{How many decimal places to report (Default 2)}

\item{report}{If "html", then show results in browser (default = "markdown")}

\item{means}{Whether to include means in the summary (TRUE)}

\item{residuals}{Whether to include residuals in the summary (TRUE)}

\item{uncertainty}{What type of uncertainty/inference packaging to report: \code{"SE"} (default SEs from the fit), \code{"RobustSE"} (engine-appropriate robust inference: ML casewise sandwich SEs + robust AFIs; for WLS, moment-sandwich SEs + robust WLS AFIs when available), \code{"CI"} (profile likelihood CIs), or \code{"none"}.}

\item{filter}{whether to show significant paths (SIG) or NS paths (NS) or all paths (ALL)}

\item{RMSEA_CI}{Whether to compute the CI on RMSEA (Defaults to FALSE)}

\item{SE}{Deprecated. Please use \code{uncertainty} instead.}

\item{matrixAddresses}{Whether to show "matrix address" columns (Default = FALSE)}

\item{...}{Other parameters to control model summary}
}
\value{
\itemize{
\item parameterTable returned invisibly, if estimates requested
}
}
\description{
Report the fit of a model in a compact form suitable for a journal.
It reports parameters in a markdown or html table (optionally standardized), and fit indices
RMSEA (an absolute fit index, comparing the model to a perfect model) and CFI and TLI (incremental fit indices comparing a model with the worst fit).
}
\details{
\code{umxSummary} alerts you when model fit is worse than accepted criterion (TLI >= .95 and RMSEA <= .06; (Hu & Bentler, 1999; Yu, 2002).

Note: For some (multi-group) models, you will need to fall back on \code{\link[=summary]{summary()}}

\strong{Robust inference (\code{uncertainty = "RobustSE"})}

This option means \strong{engine-appropriate robust inference}, not one sandwich formula for every fit type:
\itemize{
\item \strong{ML / FIML (raw continuous):} casewise score sandwich SEs via \code{\link[OpenMx:imxRobustSE]{OpenMx::imxRobustSE()}}, plus robust CFI/TLI/RMSEA via \code{\link[=xmu_robust_ML_fit]{xmu_robust_ML_fit()}} (Yuan–Bentler / Brosseau-Liard & Savalei scaling).
\item \strong{WLS / DWLS / ULS:} do \strong{not} use the ML casewise sandwich. Parameter SEs are the usual \strong{WLS/GMM moment sandwich} already returned by OpenMx (\eqn{W}, \eqn{\Delta}, \eqn{\Gamma}). Robust CFI/TLI/RMSEA are applied when \code{output$implied_jacobian} is present via \code{\link[=xmu_robust_WLS_fit]{xmu_robust_WLS_fit()}} (Satorra–Bentler 2010 for continuous WLS; Savalei 2021 catML for ordinal WLS). A statistical note states that SEs are WLS asymptotic (moment sandwich).
}

\strong{Robust Fit Statistics for ML}
Standard Maximum Likelihood (ML) is powerful and efficient, with χ2, CFI, TLI, and RMSEA measures of fit. Non-normality and heteroscedasticity distort the sampling distribution of the scores and the ML test statistic itself, necessitating changes to SEs and fit statistics. For ML, \code{uncertainty = "RobustSE"} implements the sandwich estimator for SEs and MLR-style fit scaling (Yuan–Bentler / Savalei 2018-style robust indices).

\strong{Robust Fit Statistics for WLS/DWLS (The GenomicMx Ecosystem)}

Simulation work shows CFI/TLI behave differently under DWLS/WLS than under ML
(Shi et al., 2020): incremental indices use an independence baseline under the
weight matrix, so conventional cutoffs (e.g. CFI > 0.95) break down for continuous WLS
and Genomic SEM. Prefer \strong{SRMR} and residual inspection for absolute fit; use
\code{\link[=umxCompare]{umxCompare()}} for nested tests. See \code{?umxCompare} for the full guidance.

\emph{Implementation:} when a Jacobian is available, \code{umxSummary} routes WLS models
through \code{\link[=xmu_robust_WLS_fit]{xmu_robust_WLS_fit()}} (independence baseline + trace scaling). Parameter SEs
remain the OpenMx WLS moment sandwich (see \code{uncertainty = "RobustSE"} above). Robust
CFI/TLI/RMSEA are \strong{reported} for continuous WLS but should be treated as descriptive
(no Hu–Bentler cutoffs). Genomic SEM has the same rule, with even less trust in RMSEA
under bookkeeping N.

\strong{Two statistic families (by design).} WLS output separates omnibus testing from fit indices:
\itemize{
\item \strong{Displayed \eqn{\chi^2} and \eqn{p}} — Satorra–Bentler (2010) scaled WLS test when robust fit succeeds (continuous and ordinal).
\item \strong{Displayed CFI, TLI, RMSEA} — robust/scaled indices; interpretation depends on continuous vs ordinal (below).
}

\strong{Continuous WLS} (all manifests numeric): SB (2010) scaled pipeline. Printed note: no conventional cutoffs; prefer SRMR; nested Strict SB via \code{\link[=umxCompare]{umxCompare()}}.

\strong{Ordinal / categorical WLS} (at least one \code{ordered}/\code{factor} on raw data): robust CFI/TLI/RMSEA use \strong{Savalei (2021)} catML corrections; \strong{Hu & Bentler (1999) cutoffs may apply to those robust indices}. Display \eqn{\chi^2}/\eqn{p} remain SB-scaled WLS omnibus tests—not catML-scaled.

Requires \code{implied_jacobian} after \code{mxRun} on WLS. If robust computation fails, falls back to OpenMx \code{summary()} with a note.

\strong{CIs and Identification}
This function uses the standard errors reported by OpenMx to produce the CIs you see in umxSummary.
These are used to derive confidence intervals based on the formula 95\%CI = estimate +/- 1.96*SE.

Sometimes SEs appear NA. This may reflect a model which is not identified (see \url{http://davidakenny.net/cm/identify.htm}).
This can include empirical under-identification - for instance two factors
that are essentially identical in structure. Use \code{\link[OpenMx:mxCheckIdentification]{OpenMx::mxCheckIdentification()}} to check identification.

Solutions: If there are paths estimated at or close to zero, this suggests that fixing one or two of
these to zero may fix the standard error calculation.

If factor loadings can flip sign and provide identical fit, this creates another form of
under-identification and can break confidence interval estimation.
\emph{Solution}: Fixing a factor loading to 1 and estimating factor variances can help here.
}
\examples{
\dontrun{
require(umx)
data(demoOneFactor)
manifests = names(demoOneFactor)
m1 = umxRAM("One Factor", data = demoOneFactor, type = "cov",
 umxPath("G", to = manifests),
 umxPath(var = manifests),
 umxPath(var = "G", fixedAt = 1)
)
umxSummary(m1, std = TRUE)
# output as latex
umx_set_table_format("latex")
umxSummary(m1, std = TRUE)
umx_set_table_format("markdown")
# output as raw
umxSummary(m1, std = FALSE)

# switch to a raw data model
m1 = umxRAM("One Factor", data = demoOneFactor[1:100, ],
 umxPath("G", to = manifests),
 umxPath(v.m. = manifests),
 umxPath(v1m0 = "G")
)
umxSummary(m1, std = TRUE, filter = "NS")
}

}
\references{
\itemize{
\item Hu, L., & Bentler, P. M. (1999). Cutoff criteria for fit indexes in covariance
structure analysis: Conventional criteria versus new alternatives. \emph{Structural Equation Modeling}, \strong{6}, 1-55.
\item Satorra, A., & Bentler, P. M. (2010). Ensuring positiveness of the scaled difference chi-square test statistic. \emph{Psychometrika}, \strong{75}(2), 243-248.
\item Satorra, A. and Bentler P. M. (1994). Corrections to test statistics and standard errors in covariance structure analysis. Latent variables analysis: Applications for developmental research. A. von Eye and C. C. Clogg, Sage: 399-419.
\item Savalei, V. (2018). On the computation of the RMSEA and CFI from the mean-and-variance corrected test statistic with nonnormal data in SEM. \emph{Multivariate Behavioral Research}, \strong{53}(3), 419--429.
\item Savalei, V. (2021). Improving fit indices in SEM with categorical data. \emph{Multivariate Behavioral Research}, \strong{56}(3), 390--407.
\item Brosseau-Liard, P. E., & Savalei, V. (2012). Adjusting incremental fit indices for nonnormality. \emph{Multivariate Behavioral Research}, \strong{47}(5), 647--677.
\item Yu, C.Y. (2002). Evaluating cutoff criteria of model fit indices for latent variable models
with binary and continuous outcomes. University of California, Los Angeles.
Retrieved from \url{https://www.statmodel.com/download/Yudissertation.pdf}
\item Yuan, K.-H. and P. M. Bentler (2000). "5. Three Likelihood-Based Methods for Mean and Covariance Structure Analysis with Nonnormal Missing Data." Sociological Methodology 30(1): 165-200.
}

\url{https://tbates.github.io}
}
\seealso{
\itemize{
\item \code{\link[=umxRAM]{umxRAM()}}, \code{\link[=xmu_robust_WLS_fit]{xmu_robust_WLS_fit()}}
}

Other Summary functions:
\code{\link[=umxSummaryACEcov]{umxSummaryACEcov()}},
\code{\link[=umxSummaryCP]{umxSummaryCP()}},
\code{\link[=umxSummaryGxE]{umxSummaryGxE()}},
\code{\link[=umxSummaryIP]{umxSummaryIP()}},
\code{\link[=umxSummaryMRDoC]{umxSummaryMRDoC()}}
}
\concept{Summary functions}
