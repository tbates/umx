% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/misc_and_utility.R
\name{umx_make_GxE_data}
\alias{umx_make_GxE_data}
\title{Simulate ground-truth Purcell GxE twin data for benchmarking umxGxE}
\usage{
umx_make_GxE_data(
  nMZpairs = 2500L,
  nDZpairs = 2500L,
  a = 0.5,
  c = 0.3,
  e = 0.6,
  am = 0.15,
  cm = 0,
  em = 0,
  betaAge = NULL,
  targetAgeCor = 0.2,
  seed = 1L,
  sharedModerator = TRUE,
  sharedAge = TRUE,
  dzAr = 0.5,
  mu = 0,
  betaMod = 0,
  ageMean = 40,
  ageSd = 10,
  clipModerator = TRUE
)
}
\arguments{
\item{nMZpairs}{Number of MZ pairs (default 2500).}

\item{nDZpairs}{Number of DZ pairs (default 2500; total N = 5000 pairs).}

\item{a}{Main additive path at \(M = 0\) (default 0.50).}

\item{c}{Main common-environment path at \(M = 0\) (default 0.30).}

\item{e}{Main unique-environment path at \(M = 0\) (default 0.60).}

\item{am}{Additive moderation slope \(a_m\) / a-prime (default 0.15).}

\item{cm}{Common-environment moderation slope (default 0).}

\item{em}{Unique-environment moderation slope (default 0).}

\item{betaAge}{Mean slope of age on \code{outcomeAge}. If \code{NULL} (default),
chosen so that the correlation r(outcomeAge_T1, age_T1) is approximately \code{targetAgeCor}.}

\item{targetAgeCor}{Target correlation between \code{outcomeAge} and \code{age} when calibrating \code{betaAge} (default 0.2).}

\item{seed}{Random seed (default 1).}

\item{sharedModerator}{If TRUE (default), one moderator draw per family.}

\item{sharedAge}{If TRUE (default), one age draw per family.}

\item{dzAr}{DZ additive genetic correlation (default 0.5).}

\item{mu}{Intercept for both outcomes (default 0).}

\item{betaMod}{Mean effect of moderator on both outcomes (default 0).}

\item{ageMean}{Mean of age in years (default 40).}

\item{ageSd}{SD of age (default 10).}

\item{clipModerator}{If TRUE (default), clip moderator draws to the interval from -3 to 3.}
}
\value{
data.frame with columns \code{zygosity}, \code{outcome_T1/T2},
\code{outcomeAge_T1/T2}, \code{mod_T1/T2}, \code{age_T1/T2}. Attribute
\code{"truth"} is a named list of generative parameters (\code{a}, \code{c},
\code{e}, \code{am}, \code{cm}, \code{em}, \code{betaAge_outcomeAge},
\code{cor_outcomeAge_age}, sample sizes, seed, etc.). Base R and
\code{umxGxE} ignore attributes on the data frame; use
\code{attr(df, "truth")} only for comparison to estimates.
}
\description{
Generates a wide twin data frame with known path coefficients in the
Purcell (2002) moderated ACE form used by \code{\link[=umxGxE]{umxGxE()}}:
\(a_i = a + a_m M\), and similarly for \(c\) and \(e\).
}
\details{
Two outcomes share the same latent ACE draws and moderation:
\describe{
\item{\code{outcome}}{GxE only (no age mean effect).}
\item{\code{outcomeAge}}{Same as \code{outcome} plus a mean effect of \code{age}
calibrated so the sample correlation with age is approximately \code{targetAgeCor}.}
}
Family-level moderator and age are shared by co-twins (\code{mod_T1 == mod_T2},
\code{age_T1 == age_T2}) by default.
}
\examples{
df = umx_make_GxE_data(nMZpairs = 200, nDZpairs = 200, seed = 1)
str(df)
truth = attr(df, "truth")
truth$am
truth$a
\dontrun{
m1 = umxGxE(selDVs = "outcome", selDefs = "mod", sep = "_T",
  data = df, mzData = "MZ", dzData = "DZ", tryHard = "yes")
}
}
\references{
Purcell, S. (2002). Variance components models for gene-environment
interaction in twin analysis. Twin Research, 5, 554-571. \doi{10.1375/twin.5.6.554}
}
\seealso{
\code{\link[=umxGxE]{umxGxE()}}, \code{\link[=umx_make_TwinData]{umx_make_TwinData()}}

Other Data Functions:
\code{\link[=noNAs]{noNAs()}},
\code{\link[=prolific_anonymize]{prolific_anonymize()}},
\code{\link[=prolific_check_ID]{prolific_check_ID()}},
\code{\link[=prolific_read_demog]{prolific_read_demog()}},
\code{\link[=prolific_scoring_stub]{prolific_scoring_stub()}},
\code{\link{umx}},
\code{\link[=umxFactor]{umxFactor()}},
\code{\link[=umxGSEM_sim_snps]{umxGSEM_sim_snps()}},
\code{\link[=umxHetCor]{umxHetCor()}},
\code{\link[=umx_as_numeric]{umx_as_numeric()}},
\code{\link[=umx_cont_2_quantiles]{umx_cont_2_quantiles()}},
\code{\link[=umx_lower2full]{umx_lower2full()}},
\code{\link[=umx_make_MR_data]{umx_make_MR_data()}},
\code{\link[=umx_make_TwinData]{umx_make_TwinData()}},
\code{\link[=umx_make_fake_data]{umx_make_fake_data()}},
\code{\link[=umx_make_raw_from_cov]{umx_make_raw_from_cov()}},
\code{\link[=umx_merge_randomized_columns]{umx_merge_randomized_columns()}},
\code{\link[=umx_polychoric]{umx_polychoric()}},
\code{\link[=umx_polypairwise]{umx_polypairwise()}},
\code{\link[=umx_polytriowise]{umx_polytriowise()}},
\code{\link[=umx_read_lower]{umx_read_lower()}},
\code{\link[=umx_rename]{umx_rename()}},
\code{\link[=umx_reorder]{umx_reorder()}},
\code{\link[=umx_score_scale]{umx_score_scale()}},
\code{\link[=umx_select_valid]{umx_select_valid()}},
\code{\link[=umx_stack]{umx_stack()}},
\code{\link[=umx_strings2numeric]{umx_strings2numeric()}}
}
\concept{Data Functions}
