% Generated by roxygen2 (4.1.1): do not edit by hand
% Please edit documentation in R/fit_and_reporting.r
\name{umx_report_Anova}
\alias{umx_report_Anova}
\title{umx_report_Anova}
\usage{
umx_report_Anova(model1, model2 = NULL, raw = TRUE, format = c("kable",
  "plain"), printDIC = FALSE)
}
\arguments{
\item{model1}{An \code{\link{lm}} model to make a table from}

\item{model2}{An (optional) second \code{\link{lm}} model to compare to model 1}

\item{raw}{Should the raw table also be output? (allows checking that nothing crazy is going on: default = TRUE)}

\item{format}{markdown (kable) or plain text output}

\item{printDIC}{A Boolean toggle whether you want AIC-type fit change table printed}
}
\description{
umx_report_Anova is a convenience function to format results for journals. There are others. But I made this one.
If you give it the output of an lm, it runs anova() and QuantPsyc::lm.beta(), and
generates a text-format report of the F values in an ANOVA. e.g. "F(495,1) = 0.002"
Along with a a regression table...
}
\details{
Alternatively if you fill in the optional second model, it compares them (just like \code{\link{umxCompare}})
}
\examples{
m1 = lm(mpg ~ cyl + disp, data = mtcars)
umx_report_Anova(m1)
m1 = lm(mpg~ cyl + wt, data = mtcars)
umxAnova(m1)
m2 = lm(mpg~ cyl, data = mtcars)
umxAnova(m2)
umxAnova(anova(m1, m2))
}
\references{
- \url{http://www.github.com/tbates/umx}
}
\seealso{
- \code{\link{umxSummary}}, \code{\link{umxCompare}}, \code{\link{anova}}

Other Reporting Functions: \code{\link{umxStandardizeACE}};
  \code{\link{umx_APA_CI}}; \code{\link{umx_APA_pval}};
  \code{\link{umx_aggregate}}; \code{\link{umx_print}};
  \code{\link{umx_show}}; \code{\link{umx_time}};
  \code{\link{umx}}, \code{\link{umx-package}}
}

