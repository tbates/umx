% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/xmuGSEM.R
\name{xmu_gsem_expand_snp}
\alias{xmu_gsem_expand_snp}
\title{Expand SNP Summary Statistics into a Full Covariance Matrix}
\usage{
xmu_gsem_expand_snp(
  covstruc,
  beta_i,
  se_i,
  varSNP_i,
  varSNPSE2,
  GC = "standard",
  coords = NULL
)
}
\arguments{
\item{covstruc}{A list containing the base trait covariance matrix \verb{$S} and its asymptotic covariance \verb{$V}.}

\item{beta_i}{Numeric vector of length $k$. The GWAS betas (effects) of the SNP on the $k$ traits.}

\item{se_i}{Numeric vector of length $k$. The GWAS standard errors for the SNP on the $k$ traits.}

\item{varSNP_i}{Numeric. The variance of the SNP (typically \eqn{2 \times MAF \times (1 - MAF)}).}

\item{varSNPSE2}{Numeric. The asymptotic variance of the SNP's variance estimate.}

\item{GC}{Character. Genomic control method applied to the cross-trait sampling covariances.
Options are \code{"standard"}, \code{"conserv"}, or \code{"none"}.}

\item{coords}{Optional matrix mapping coordinates for the asymptotic covariance matrix.}
}
\value{
A list containing:
\itemize{
\item \code{S}: The expanded \eqn{(k+1) \times (k+1)} covariance matrix.
\item \code{V}: The expanded \eqn{((k+1)(k+2)/2) \times ((k+1)(k+2)/2)} asymptotic covariance matrix.
}
}
\description{
Takes a baseline covariance structure (\code{covstruc}) of traits and incorporates the
summary statistics of a single SNP to produce a combined \eqn{(k+1) \times (k+1)}
covariance matrix (\code{S}) and corresponding asymptotic covariance matrix (\code{V}).
}
